<phone:PhoneApplicationPage 
    x:Class="WordPress.EditPostPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Converters="clr-namespace:WordPress.Converters"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d" d:DesignHeight="768" d:DesignWidth="480"
    shell:SystemTray.IsVisible="True">

    <phone:PhoneApplicationPage.Resources>
        <Converters:CategoryContentConverter x:Key="CategoryContentConverter"/>
    </phone:PhoneApplicationPage.Resources>
    
    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Style="{StaticResource LayoutRootStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--TitlePanel contains the name of the application and page title-->
        <StackPanel x:Name="TitlePanel" Grid.Row="0" Margin="12,17,0,28">
            <Grid Margin="0,0,8,0">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Image Source="Images/blue-s.png" Stretch="None" Width="50" Margin="0" HorizontalAlignment="Left" RenderTransformOrigin="0.52,-0.18" d:LayoutOverrides="Height"/>
                <TextBlock x:Name="ApplicationTitle" Text="{Binding Path=ApplicationTitle, Source={StaticResource StringTable}}" Style="{StaticResource PhoneTextNormalStyle}" Margin="10,0,0,0" HorizontalAlignment="Left" Grid.Column="1" d:LayoutOverrides="Height" VerticalAlignment="Center">
            		<TextBlock.Foreground>
            			<SolidColorBrush Color="{StaticResource WordPressBlue}"/>
            		</TextBlock.Foreground>
                </TextBlock>
            </Grid>

            <TextBlock x:Name="PageTitle" Text="{Binding Path=PageTitles.EditPost, Source={StaticResource StringTable}}" Margin="9,-7,0,0" Style="{StaticResource PhoneTextTitle1Style}">
            <TextBlock.Foreground>
                <SolidColorBrush Color="{StaticResource WordPressGrey}"/>
            </TextBlock.Foreground>
            </TextBlock>
        </StackPanel>

        <!--ContentPanel - place additional content here-->
        <ScrollViewer Grid.Row="1">
            <StackPanel>
                <TextBlock Text="{Binding Path=ControlsText.PostContent, Source={StaticResource StringTable}}" Style="{StaticResource LabelTextBlockStyle}"/>
                <TextBox x:Name="titleTextBox" Text="{Binding Title, Mode=TwoWay}" Style="{StaticResource TextBoxStyle}" Template="{StaticResource FocusedTextBoxTemplate}"/>
                <TextBox x:Name="contentTextBox" Text="{Binding Description, Mode=TwoWay}" MaxHeight="400" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Style="{StaticResource TextBoxStyle}" Template="{StaticResource FocusedTextBoxTemplate}"/>
                <StackPanel Orientation="Horizontal">
                    <ToggleButton x:Name="boldToggleButton" Content="{Binding Path=ControlsText.Bold, Source={StaticResource StringTable}}" ClickMode="Press" Click="OnBoldToggleButtonClick" Style="{StaticResource BasicToggleButtonStyle}"/>
                    <ToggleButton x:Name="italicToggleButton" Content="{Binding Path=ControlsText.Italic, Source={StaticResource StringTable}}" FontStyle="Italic" Style="{StaticResource BasicToggleButtonStyle}"/>
                    <ToggleButton x:Name="underlineToggleButton" Content="{Binding Path=ControlsText.Underline, Source={StaticResource StringTable}}" Style="{StaticResource BasicToggleButtonStyle}" />
                    <ToggleButton x:Name="strikeThroughToggleButton" Content="{Binding Path=ControlsText.StrikeThrough, Source={StaticResource StringTable}}" Style="{StaticResource BasicToggleButtonStyle}"/>
                    <Button x:Name="linkButton" Content="{Binding Path=ControlsText.Link, Source={StaticResource StringTable}}" Style="{StaticResource BasicButtonStyle}"/>
                    <ToggleButton x:Name="blockQuoteButton" Content="{Binding Path=ControlsText.BlockQuote, Source={StaticResource StringTable}}" Style="{StaticResource BasicToggleButtonStyle}"/>
                </StackPanel>
                <TextBlock Text="{Binding Path=ControlsText.Media, Source={StaticResource StringTable}}" Style="{StaticResource LabelTextBlockStyle}"/>
                <Button Content="{Binding Path=ControlsText.AddMedia, Source={StaticResource StringTable}}" HorizontalAlignment="Left" Style="{StaticResource BasicButtonStyle}"/>
                <TextBlock Text="{Binding Path=ControlsText.TagsAndCategories, Source={StaticResource StringTable}}" Style="{StaticResource LabelTextBlockStyle}"/>
                <TextBox x:Name="tagsTextBox" Text="{Binding MtKeyWords, Mode=TwoWay}" Style="{StaticResource TextBoxStyle}" Template="{StaticResource FocusedTextBoxTemplate}" />
                <Button x:Name="selectCategoriesButton" HorizontalAlignment="Left" Style="{StaticResource BasicButtonStyle}"
                        Click="OnSelectCategoriesButtonClick" MinHeight="70" MinWidth="70" >
                    <TextBlock x:Name="categoriesTextBlock" TextWrapping="Wrap" Text="{Binding Path=Categories, Mode=OneWay, Converter={StaticResource CategoryContentConverter}}" FontSize="26"/>
                </Button>
                        
                <TextBlock Text="{Binding Path=ControlsText.Status, Source={StaticResource StringTable}}" Style="{StaticResource LabelTextBlockStyle}"/>
                <ToggleButton x:Name="publishToggleButton" Content="{Binding Path=ControlsText.Publish, Source={StaticResource StringTable}}" HorizontalAlignment="Left" Style="{StaticResource BasicToggleButtonStyle}"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>

</phone:PhoneApplicationPage>
